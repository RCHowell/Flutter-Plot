import 'package:test/test.dart';

// import 'package:flutter_plot/plot.dart';

void main() {
  test('Tests a plot', () {

    // I honestly have no idea how one would go about testing a plotting tool programmatically, but I have
    // visually tested it many many times so take that for what you find it to be worth.

    // final plot = new Plot();
    // expect(a, b); expect a == b

  });
}
